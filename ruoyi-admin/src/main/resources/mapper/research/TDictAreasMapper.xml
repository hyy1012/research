<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.research.mapper.TDictAreasMapper">
    
    <resultMap type="TDictAreas" id="TDictAreasResult">
        <result property="id"    column="id"    />
        <result property="cityCode"    column="city_code"    />
        <result property="areaCode"    column="area_code"    />
        <result property="areaName"    column="area_name"    />
    </resultMap>

    <sql id="selectTDictAreasVo">
        select id, city_code, area_code, area_name from t_dict_areas
    </sql>

    <select id="selectTDictAreasList" parameterType="TDictAreas" resultMap="TDictAreasResult">
        <include refid="selectTDictAreasVo"/>
        <where>  
            <if test="cityCode != null  and cityCode != ''"> and city_code = #{cityCode}</if>
            <if test="areaCode != null  and areaCode != ''"> and area_code = #{areaCode}</if>
            <if test="areaName != null  and areaName != ''"> and area_name like concat('%', #{areaName}, '%')</if>
        </where>
    </select>
    
    <select id="selectTDictAreasById" parameterType="Long" resultMap="TDictAreasResult">
        <include refid="selectTDictAreasVo"/>
        where id = #{id}
    </select>

    <insert id="insertTDictAreas" parameterType="TDictAreas" useGeneratedKeys="true" keyProperty="id">
        insert into t_dict_areas
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="cityCode != null">city_code,</if>
            <if test="areaCode != null">area_code,</if>
            <if test="areaName != null">area_name,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="cityCode != null">#{cityCode},</if>
            <if test="areaCode != null">#{areaCode},</if>
            <if test="areaName != null">#{areaName},</if>
         </trim>
    </insert>

    <update id="updateTDictAreas" parameterType="TDictAreas">
        update t_dict_areas
        <trim prefix="SET" suffixOverrides=",">
            <if test="cityCode != null">city_code = #{cityCode},</if>
            <if test="areaCode != null">area_code = #{areaCode},</if>
            <if test="areaName != null">area_name = #{areaName},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteTDictAreasById" parameterType="Long">
        delete from t_dict_areas where id = #{id}
    </delete>

    <delete id="deleteTDictAreasByIds" parameterType="String">
        delete from t_dict_areas where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>